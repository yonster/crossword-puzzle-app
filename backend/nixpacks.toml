[phases.setup]
nixpkgs = ['python3', 'gcc']

[phases.install]
cmds = ['python -m venv --copies /opt/venv', 'pip install -r requirements.txt']

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'